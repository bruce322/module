name: Block Bad UDP
desc: >-
  拦截被DNS污染和针对YouTube的UDP请求，对其他UDP请求仍按设定规则匹配和转发，尽可能保留完整的UDP体验。
  兼容YouTube去广告覆写

script:
  shortcuts:
    S-BAD-UDP: network == 'udp' and ('googlevideo' in host or 'youtube' in host)

rules:
  - SCRIPT,S-BAD-UDP,REJECT,no-resolve
  
